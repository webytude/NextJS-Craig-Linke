import React from 'react'

export default function MediaWithTopBottomContent({ data }) {
  const { Title, TopContent, BottomContent, Media } = data;

  return (
    <div>
      MediaWithTopBottomContent
    </div>
  )
}
